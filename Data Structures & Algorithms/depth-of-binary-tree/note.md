## Mistakes I had done: 
1. When writing code (BFS Solution), don't use `q.size()` inside `for loop`'s condition because it will change every time after `pop()`. Use `len = q.size()` before writing the loop.
 
